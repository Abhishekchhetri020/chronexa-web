// [vite-esm] imports auto-generated by the 2026-07 Vite migration.
import "./state.js";

/* Studio v3 shell — wraps the existing app DOM in the 3-column layout
 * (sidebar nav + topbar with wordmark + ⌘K palette + main + right rail
 * with solver/faults + status bar). DOM-only; reuses the existing
 * EntityX.open() and event surface so no module changes required.
 *
 * Auto-mounts on DOMContentLoaded. Idempotent.
 */
(function (global) {
  "use strict";
  const APP = global.APP = global.APP || {};

  let mounted = false;
  let lastFaults = [];

  function el(tag, attrs, ...kids) {
    const n = document.createElement(tag);
    if (attrs) for (const k in attrs) {
      const v = attrs[k];
      if (v == null) continue;
      if (k === "class") n.className = v;
      else if (k.startsWith("on") && typeof v === "function") n.addEventListener(k.slice(2), v);
      else n.setAttribute(k, v);
    }
    for (const c of kids) if (c != null) n.appendChild(typeof c === "string" ? document.createTextNode(c) : c);
    return n;
  }
  function fire(name, detail) {
    global.dispatchEvent(new CustomEvent(name, { detail: detail || {} }));
  }

  // Sidebar nav entries — each fires an app:open-entity event or routes
  // to an existing handler. Replaces the ribbon menu drilling.
  // SVG icons for better visual hierarchy
  const NAV = [
    { section: "Workspace" },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M9 3v18M3 9h18"/></svg>', label: "Editor",    active: true, on: () => focusEditor() },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>', label: "Reports",   on: () => fire("app:print-preview") },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>', label: "Substitutions", on: () => fire("app:substitutions") },
    { section: "Entities" },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/><path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"/></svg>', label: "Subjects",   on: () => fire("app:open-entity", { kind: "subjects" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/></svg>', label: "Teachers",   on: () => fire("app:open-entity", { kind: "teachers" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>', label: "Classes",    on: () => fire("app:open-entity", { kind: "classes" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg>', label: "Rooms",      on: () => fire("app:open-entity", { kind: "classrooms" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>', label: "Lessons",    on: () => fire("app:open-entity", { kind: "lessons" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M16 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="8.5" cy="7" r="4"/><line x1="20" y1="8" x2="20" y2="14"/><line x1="23" y1="11" x2="17" y2="11"/></svg>', label: "Divisions",  on: () => fire("app:open-entity", { kind: "divisions" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>', label: "Grades",     on: () => fire("app:open-entity", { kind: "grades" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>', label: "Students",   on: () => fire("app:open-entity", { kind: "students" }) },
    { section: "Structure" },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>', label: "Bells",      on: () => fire("app:open-entity", { kind: "bells" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>', label: "Days",       on: () => fire("app:open-entity", { kind: "days" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/><path d="M8 14h.01M12 14h.01M16 14h.01M8 18h.01M12 18h.01M16 18h.01"/></svg>', label: "Weeks",      on: () => fire("app:open-entity", { kind: "weeks" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/></svg>', label: "Terms",      on: () => fire("app:open-entity", { kind: "terms" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg>', label: "Buildings",  on: () => fire("app:open-entity", { kind: "buildings" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>', label: "Relations",  on: () => fire("app:open-entity", { kind: "relations" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>', label: "School settings", on: () => fire("app:open-entity", { kind: "school" }) },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="4" y1="21" x2="4" y2="14"/><line x1="4" y1="10" x2="4" y2="3"/><line x1="12" y1="21" x2="12" y2="12"/><line x1="12" y1="8" x2="12" y2="3"/><line x1="20" y1="21" x2="20" y2="16"/><line x1="20" y1="12" x2="20" y2="3"/><line x1="1" y1="14" x2="7" y2="14"/><line x1="9" y1="8" x2="15" y2="8"/><line x1="17" y1="16" x2="23" y2="16"/></svg>', label: "Solver params",  on: () => fire("app:solver-params") },
    { section: "Files" },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>', label: "Import…",    on: () => fire("app:import-timetable-xml") },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>', label: "Export XML", on: () => fire("app:export-timetable-xml") },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>', label: "Calendar (ICS)", on: () => fire("app:export-ics") },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>', label: "Save",      on: () => fire("app:save") },
    { svg: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>', label: "Snapshots", on: () => fire("app:open-snapshot") },
  ];

  function buildSidebar() {
    const side = el("aside", { class: "chrx-sidebar" });
    for (const item of NAV) {
      if (item.section) {
        side.appendChild(el("div", { class: "chrx-side-section" }, item.section));
        continue;
      }
      const link = el("a", { class: "chrx-side-link" + (item.active ? " is-active" : ""),
        onclick: (e) => { e.preventDefault(); try { item.on(); } catch (er) { console.error(er); } } });
      // Support both SVG icons (new) and text icons (legacy)
      const iconSpan = el("span", { class: "chrx-side-link__icon" });
      if (item.svg) {
        iconSpan.innerHTML = item.svg;
      } else {
        iconSpan.textContent = item.icon || item.label[0];
      }
      link.appendChild(iconSpan);
      link.appendChild(document.createTextNode(item.label));
      side.appendChild(link);
    }
    return side;
  }

  function buildWordmark() {
    return el("header", { class: "chrx-wordmark" },
      el("div", { class: "chrx-stamp" }, "C"),
      el("div", { class: "chrx-word" }, "Chronexa"));
  }

  function buildTopbar() {
    const hamburgerSvg = el("span", { style: "display:inline-flex; align-items:center; justify-content:center; width:16px; height:16px;" });
    hamburgerSvg.innerHTML = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:block;"><line x1="3" y1="12" x2="21" y2="12"></line><line x1="3" y1="6" x2="21" y2="6"></line><line x1="3" y1="18" x2="21" y2="18"></line></svg>';

    const sideBtn = el("button", { class: "chrx-shell-toggle", "data-toggle": "side",
      title: "Toggle sidebar  [", "aria-label": "Toggle sidebar",
      onclick: () => togglePanel("side") }, hamburgerSvg);

    const crumbs = el("div", { class: "chrx-crumbs", id: "chrx-crumbs" });
    crumbs.appendChild(el("span", null, "Untitled"));
    crumbs.appendChild(el("span", { class: "chrx-crumbs__sep" }, "/"));
    crumbs.appendChild(el("span", { class: "chrx-crumbs__current" }, "Editor"));

    // Host for the editor quick-tools (Lessons / density / skin / color /
    // perspective + unplaced count). main.js moves #editor-quick-tools in here
    // when the editor is active, so those controls sit next to the breadcrumb
    // and the grid reclaims that row for taller cards.
    const editorToolsSlot = el("div", { class: "chrx-topbar-editor-tools", id: "chrx-topbar-editor-slot" });

    const search = el("div", { class: "chrx-kbd-search",
      onclick: () => openPalette() });
    const searchIc = el("span", { class: "chrx-kbd-search__icon" });
    searchIc.innerHTML = global.ChrxIcons ? global.ChrxIcons.svg("search", 14) : "";
    search.appendChild(searchIc);
    search.appendChild(el("span", null, "Search anything…"));
    search.appendChild(el("span", { class: "chrx-kbd-search__kbd" }, "⌘K"));

    // Generate / Test: delegate to the legacy #cta-generate / #cta-test
    // click handlers (the real wiring lives in index.html bind block).
    // Firing an event alone doesn't work — nothing listens to app:generate.
    function clickLegacy(id, eventName) {
      const legacy = document.getElementById(id);
      if (legacy && !legacy.disabled) { legacy.click(); return; }
      fire(eventName);
    }
    const actions = el("div", { class: "chrx-action-row", style: "display:flex; gap:8px" },
      el("button", { class: "chrx-btn", onclick: () => clickLegacy("cta-test", "app:test") }, "Test"),
      el("button", { class: "chrx-btn chrx-btn--primary", onclick: () => clickLegacy("cta-generate", "app:generate") }, "Generate"));

    const fsBtn = el("button", { class: "chrx-shell-toggle", "data-toggle": "fs",
      title: "Fullscreen editor  F (Esc to exit)", "aria-label": "Toggle fullscreen editor",
      onclick: () => togglePanel("fs") });
    fsBtn.innerHTML = global.ChrxIcons ? global.ChrxIcons.svg("expand", 16) : "⛶";

    return el("header", { class: "chrx-topbar" },
      sideBtn,
      crumbs,
      editorToolsSlot,
      el("div", { class: "chrx-topbar__spacer" }),
      search,
      actions,
      fsBtn);
  }

  function buildRail() {
    const rail = el("aside", { class: "chrx-rail" });

    // Solver section
    const solverSec = el("div", { class: "chrx-rail-section" });
    solverSec.appendChild(el("div", { class: "chrx-rail-section__head" }, "Solver"));
    const card = el("div", { class: "chrx-solver-card", id: "chrx-rail-solver" });
    function statRow(label, valueId, klass) {
      const r = el("div", { class: "chrx-solver-stat" });
      r.appendChild(el("span", { class: "chrx-solver-stat__label" }, label));
      r.appendChild(el("span", { class: "chrx-solver-stat__value" + (klass ? " " + klass : ""), id: valueId }, "—"));
      return r;
    }
    card.appendChild(statRow("Status",     "chrx-rail-status",     "is-good"));
    card.appendChild(statRow("Placed",     "chrx-rail-placed"));
    card.appendChild(statRow("Conflicts",  "chrx-rail-conflicts",  "is-good"));
    card.appendChild(statRow("Soft score", "chrx-rail-soft"));
    card.appendChild(statRow("Wall time",  "chrx-rail-wall"));
    solverSec.appendChild(card);
    rail.appendChild(solverSec);

    // Faults section
    const faultsSec = el("div", { class: "chrx-rail-section" });
    faultsSec.appendChild(el("div", { class: "chrx-rail-section__head" }, "Currently stuck"));
    faultsSec.appendChild(el("ul", { class: "chrx-faults-list", id: "chrx-rail-faults" }));
    rail.appendChild(faultsSec);

    // Quick actions
    const qaSec = el("div", { class: "chrx-rail-section" });
    qaSec.appendChild(el("div", { class: "chrx-rail-section__head" }, "Quick actions"));
    const qa = el("div", { class: "chrx-quick-actions" });
    [
      { icon: "sparkle", label: "Improve schedule", hint: "⌘I", fire: "app:improve" },
      { icon: "shield",  label: "Verify all",       hint: "⌘V", fire: "app:verify" },
      { icon: "camera",  label: "Snapshot",         hint: "⌘S", fire: "app:save-as" },
      { icon: "export",  label: "Export XML",       hint: "⌘E", fire: "app:export-timetable-xml" },
      { icon: "print",   label: "Print preview",    hint: "⌘P", fire: "app:print-preview" },
    ].forEach(a => {
      const b = el("button", { class: "chrx-quick-action",
        onclick: () => fire(a.fire) });
      const qi = el("span", { class: "chrx-quick-action__icon" });
      if (global.ChrxIcons && global.ChrxIcons.has(a.icon)) qi.innerHTML = global.ChrxIcons.svg(a.icon, 15); else qi.textContent = a.icon;
      b.appendChild(qi);
      b.appendChild(document.createTextNode(a.label));
      b.appendChild(el("span", { class: "chrx-quick-action__hint" }, a.hint));
      qa.appendChild(b);
    });
    qaSec.appendChild(qa);
    rail.appendChild(qaSec);

    return rail;
  }

  function buildStatus() {
    const right = el("div", { class: "chrx-status__right" },
      el("span", null, "Studio"),
      el("span", { class: "chrx-status__divider" }, "·"),
      el("span", null, "Abhishek"));
    return el("footer", { class: "chrx-status" },
      el("span", { class: "chrx-status__dot" }),
      el("span", { id: "chrx-status-saved" }, "Ready"),
      el("span", { class: "chrx-status__divider" }, "·"),
      el("span", null, "v " + (global.APP_VER || "dev")),
      el("span", { class: "chrx-status__divider" }, "·"),
      el("span", { id: "chrx-status-perf" }, "WASM ready"),
      right);
  }

  function focusEditor() {
    const editorBtn = document.querySelector('.step-btn[data-step="6"]:not([disabled])')
      || document.querySelector('.step-btn[data-step="6"]');
    if (editorBtn) editorBtn.click();
  }

  // ───────── Command palette (⌘K) ─────────
  function buildPalette() {
    const scrim = el("div", { class: "chrx-palette-scrim", id: "chrx-palette",
      onclick: (e) => { if (e.target.id === "chrx-palette") closePalette(); } });
    const box = el("div", { class: "chrx-palette" });
    const input = el("input", { type: "text", placeholder: "Type a name…",
      oninput: (e) => renderPaletteList(e.target.value) });
    const list = el("div", { class: "chrx-palette-list", id: "chrx-palette-list" });
    box.appendChild(input);
    box.appendChild(list);
    scrim.appendChild(box);
    document.body.appendChild(scrim);
    scrim._input = input;
    scrim._list  = list;
    return scrim;
  }
  function paletteItems() {
    const items = [];
    for (const n of NAV) {
      if (n.section) continue;
      items.push({ svg: n.svg, icon: n.icon, label: n.label, sub: "open", run: n.on });
    }
    items.push({ icon: "⚡", label: "Generate timetable",  sub: "⌘G", run: () => {
      const legacy = document.getElementById("cta-generate");
      if (legacy && !legacy.disabled) { legacy.click(); return; }
      fire("app:generate");
    } });
    items.push({ icon: "🧪", label: "Test timetable",      sub: "⌘T", run: () => {
      const legacy = document.getElementById("cta-test");
      if (legacy && !legacy.disabled) { legacy.click(); return; }
      fire("app:test");
    } });
    items.push({ icon: "↻",  label: "Improve schedule",    sub: "⌘I", run: () => fire("app:improve") });
    items.push({ icon: "💾", label: "Save",                sub: "⌘S", run: () => fire("app:save") });
    items.push({ icon: "↘",  label: "Export XML",          sub: "⌘E", run: () => fire("app:export-timetable-xml") });
    items.push({ icon: "📊", label: "Statistics",          sub: "",   run: () => fire("app:statistics") });
    items.push({ icon: "💡", label: "Advisor",             sub: "",   run: () => fire("app:advisor") });
    return items;
  }
  let activeIdx = 0;
  function renderPaletteList(query) {
    const scrim = document.getElementById("chrx-palette");
    if (!scrim) return;
    const list = scrim._list;
    while (list.firstChild) list.removeChild(list.firstChild);
    const q = (query || "").toLowerCase().trim();
    const all = paletteItems();
    const hits = q
      ? all.filter(i => i.label.toLowerCase().includes(q))
      : all;
    activeIdx = 0;
    hits.slice(0, 15).forEach((it, idx) => {
      const row = el("div", { class: "chrx-palette-item" + (idx === activeIdx ? " is-active" : ""),
        onclick: () => { closePalette(); try { it.run(); } catch (e) { console.error(e); } } });
      // Support both SVG icons and text/emoji icons
      const iconSpan = el("span", { class: "chrx-palette-item__icon" });
      if (it.svg) {
        iconSpan.innerHTML = it.svg;
      } else {
        iconSpan.textContent = it.icon || "";
      }
      row.appendChild(iconSpan);
      row.appendChild(document.createTextNode(it.label));
      if (it.sub) row.appendChild(el("span", { class: "chrx-palette-item__sub" }, it.sub));
      list.appendChild(row);
    });
    scrim._hits = hits;
  }
  function openPalette() {
    let p = document.getElementById("chrx-palette");
    if (!p) p = buildPalette();
    p.classList.add("is-open");
    setTimeout(() => { p._input.value = ""; p._input.focus(); renderPaletteList(""); }, 20);
  }
  function closePalette() {
    const p = document.getElementById("chrx-palette");
    if (p) p.classList.remove("is-open");
  }
  function isTypingTarget(t) {
    if (!t) return false;
    const tag = (t.tagName || "").toLowerCase();
    return tag === "input" || tag === "textarea" || tag === "select" || t.isContentEditable;
  }
  document.addEventListener("keydown", (e) => {
    if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === "k") {
      e.preventDefault();
      openPalette();
    } else if ((e.key === "[" || e.key === "]") && !e.metaKey && !e.ctrlKey && !e.altKey && !isTypingTarget(e.target)) {
      e.preventDefault();
      togglePanel(e.key === "[" ? "side" : "rail");
    } else if ((e.key === "f" || e.key === "F") && !e.metaKey && !e.ctrlKey && !e.altKey && !isTypingTarget(e.target)) {
      e.preventDefault();
      togglePanel("fs");
    } else if (e.key === "Escape") {
      const p = document.getElementById("chrx-palette");
      if (p && p.classList.contains("is-open")) {
        closePalette();
      } else {
        const sh = document.getElementById("chrx-shell");
        if (sh?.classList.contains("is-fullscreen")) togglePanel("fs");
      }
    } else if (e.key === "Enter") {
      const p = document.getElementById("chrx-palette");
      if (p && p.classList.contains("is-open")) {
        const hits = p._hits || [];
        if (hits[activeIdx]) { closePalette(); try { hits[activeIdx].run(); } catch (er) {} }
      }
    } else if (e.key === "ArrowDown" || e.key === "ArrowUp") {
      const p = document.getElementById("chrx-palette");
      if (p && p.classList.contains("is-open")) {
        e.preventDefault();
        const items = p._list.children;
        if (!items.length) return;
        items[activeIdx]?.classList.remove("is-active");
        activeIdx = (activeIdx + (e.key === "ArrowDown" ? 1 : -1) + items.length) % items.length;
        items[activeIdx]?.classList.add("is-active");
      }
    }
  });

  // ───────── Mount ─────────
  const PANEL_STATE_KEY = "chrxShellPanels";

  function applyStoredPanels(shellEl) {
    try {
      const raw = localStorage.getItem(PANEL_STATE_KEY);
      if (!raw) return;
      const s = JSON.parse(raw);
      if (s && s.sideHidden) shellEl.classList.add("is-side-hidden");
      if (s && s.railHidden) shellEl.classList.add("is-rail-hidden");
      if (s && s.fullscreen) shellEl.classList.add("is-fullscreen");
    } catch (e) {}
  }
  function persistPanels(shellEl) {
    try {
      localStorage.setItem(PANEL_STATE_KEY, JSON.stringify({
        sideHidden: shellEl.classList.contains("is-side-hidden"),
        railHidden: shellEl.classList.contains("is-rail-hidden"),
        fullscreen: shellEl.classList.contains("is-fullscreen"),
      }));
    } catch (e) {}
  }
  function setEditorWorkspace(active) {
    const shellEl = document.getElementById("chrx-shell");
    if (!shellEl) return;
    if (active) {
      if (!shellEl.classList.contains("is-fullscreen")) {
        shellEl.dataset.autoFullscreen = "1";
        shellEl.classList.add("is-fullscreen", "is-side-hidden", "is-rail-hidden");
      }
      document.documentElement.setAttribute("data-skin", "classic");
    } else if (shellEl.dataset.autoFullscreen === "1") {
      delete shellEl.dataset.autoFullscreen;
      shellEl.classList.remove("is-fullscreen", "is-side-hidden", "is-rail-hidden");
    }
  }
  function togglePanel(which) {
    const shellEl = document.getElementById("chrx-shell");
    if (!shellEl) return;
    if (which === "fs") {
      // Fullscreen editor: compound toggle — sets/clears side + rail + chrome
      const enabling = !shellEl.classList.contains("is-fullscreen");
      delete shellEl.dataset.autoFullscreen;
      shellEl.classList.toggle("is-fullscreen", enabling);
      shellEl.classList.toggle("is-side-hidden", enabling);
      shellEl.classList.toggle("is-rail-hidden", enabling);
    } else {
      shellEl.classList.toggle(which === "side" ? "is-side-hidden" : "is-rail-hidden");
    }
    persistPanels(shellEl);
  }

  function mount() {
    if (mounted) return;
    const body = document.body;
    if (!body) return;

    const existing = Array.from(body.children).filter(n =>
      n.tagName !== "SCRIPT" && n.tagName !== "STYLE" &&
      !(n.classList && n.classList.contains("chrx-palette-scrim")));

    const shell = el("div", { class: "chrx-shell", id: "chrx-shell" });
    shell.appendChild(buildWordmark());
    shell.appendChild(buildTopbar());
    shell.appendChild(buildSidebar());
    const main = el("main", { class: "chrx-main", id: "chrx-main" });
    shell.appendChild(main);
    shell.appendChild(buildStatus());

    body.insertBefore(shell, body.firstChild);
    // Move existing app content into main
    for (const node of existing) main.appendChild(node);

    applyStoredPanels(shell);
    mounted = true;
  }

  // ───────── Live data wiring ─────────
  function updateCrumbs() {
    const crumbs = document.getElementById("chrx-crumbs");
    if (!crumbs) return;
    const name = (APP.school && APP.school.schoolName) || "Untitled";
    while (crumbs.firstChild) crumbs.removeChild(crumbs.firstChild);
    crumbs.appendChild(el("span", null, name));
    crumbs.appendChild(el("span", { class: "chrx-crumbs__sep" }, "/"));
    crumbs.appendChild(el("span", { class: "chrx-crumbs__current" }, "Editor"));
  }
  function updateSolverPanel(r) {
    function set(id, val, klass) {
      const e = document.getElementById(id);
      if (!e) return;
      e.textContent = val;
      if (klass != null) {
        e.classList.remove("is-good","is-warn","is-bad");
        if (klass) e.classList.add(klass);
      }
    }
    if (!r) {
      const sch = APP.school || {};
      set("chrx-rail-status",    sch.cards ? "READY" : "—", "");
      set("chrx-rail-placed",    String((sch.cards || []).length));
      set("chrx-rail-conflicts", "—", "");
      set("chrx-rail-soft",      "—");
      set("chrx-rail-wall",      "—");
      return;
    }
    const klass = r.status === "FEASIBLE" ? "is-good"
                : r.status === "ERROR"    ? "is-bad"
                : "is-warn";
    set("chrx-rail-status",    r.status || "—", klass);
    set("chrx-rail-placed",    String(r.stats ? r.stats.placed : 0));
    set("chrx-rail-conflicts", String(r.stats ? r.stats.hardConflicts : 0),
      r.stats && r.stats.hardConflicts === 0 ? "is-good" : "is-warn");
    set("chrx-rail-soft",      String(r.stats ? r.stats.softScore : 0));
    set("chrx-rail-wall",      r.stats ? (r.stats.durationMs / 1000).toFixed(2) + "s" : "—");
  }
  function updateFaults(items) {
    const list = document.getElementById("chrx-rail-faults");
    if (!list) return;
    lastFaults = items || [];
    while (list.firstChild) list.removeChild(list.firstChild);
    if (!lastFaults.length) {
      const li = el("li", { class: "chrx-fault-item", style: "background:transparent; border-left:0; color:var(--ink-3); font-style:italic" }, "All clear.");
      list.appendChild(li);
      return;
    }
    lastFaults.slice(0, 5).forEach(v => {
      const li = el("li", { class: "chrx-fault-item" });
      // Try to extract a subject name from description.
      const desc = (v.description || v.ruleId || "violation");
      const m = desc.match(/^([^—·:]+?)(?:\s*[—·:].*)?$/);
      const head = m ? m[1].trim() : desc;
      li.appendChild(el("span", { class: "chrx-fault-item__subj" }, head));
      if (desc !== head) li.appendChild(document.createTextNode(" — " + desc.slice(head.length).replace(/^[\s—·:]+/, "")));
      list.appendChild(li);
    });
  }
  function statusSaved(msg) {
    const s = document.getElementById("chrx-status-saved");
    if (s) s.textContent = msg;
  }

  global.addEventListener("app:school-loaded", () => { updateCrumbs(); updateSolverPanel(null); });
  document.addEventListener("step:changed", (e) => {
    const step = e.detail && e.detail.step;
    setEditorWorkspace(step === 6);
  });
  global.addEventListener("app:solver-result", (e) => updateSolverPanel(e.detail));
  global.addEventListener("app:solver-progress", (e) => {
    if (e.detail && Array.isArray(e.detail.latestViolations)) updateFaults(e.detail.latestViolations);
  });
  global.addEventListener("entity:changed", () => statusSaved("Saved · " + new Date().toLocaleTimeString().slice(0,5)));

  // Mount as early as possible
  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", mount, { once: true });
  } else {
    mount();
  }

  global.ChrxShell = { mount, openPalette, closePalette, togglePanel,
    focusEditor, setEditorWorkspace,
    refresh: () => { updateCrumbs(); updateSolverPanel(null); updateFaults(lastFaults); } };
})(window);

// [vite-esm] exports auto-generated by the 2026-07 Vite migration.
export const ChrxShell = window.ChrxShell;
